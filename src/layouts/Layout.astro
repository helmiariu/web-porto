---
import "../styles/global.css";
import SidebarMain from "../components/layout/sidebar-main.astro";
import MobileNavbarIcons from "../components/layout/mobile-navbar-icons.astro";
import MobileDropdownPanel from "../components/layout/mobile-dropdown-panel.astro";
---
<html lang="id">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script>
      (function () {
        try {
          const theme = localStorage.getItem('astro-theme');
          const isDark = theme ? theme === 'dark' : window.matchMedia?.('(prefers-color-scheme: dark)').matches;
          if (isDark) document.documentElement.classList.add('dark');
          else document.documentElement.classList.remove('dark');
        } catch (e) {
          // ignore
        }
      })();
    </script>
  </head>
  <body class="font-sans antialiased bg-[hsl(var(--background))] text-[hsl(var(--foreground))] min-h-screen overflow-x-hidden">
    <div class="mx-auto max-w-5xl px-4 md:px-8 py-8 md:py-12 flex flex-col md:flex-row gap-8 items-start">
      <SidebarMain />

      <main class="flex-1 min-w-0 pb-28 md:pb-0 w-full">
        <slot />
      </main>
    </div>

    <MobileNavbarIcons />
    <MobileDropdownPanel />
  </body>
</html>