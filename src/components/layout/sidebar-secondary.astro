---
import { publicDashboardMenu } from "../../commons/constants/navigation-menu.ts";
import ProfileSideCard from "./profile-side-card.astro";
import SidebarTheming from "./sidebar-theming.astro";
---
<nav class="space-y-5 px-4 pb-6">
  <ProfileSideCard />

  {publicDashboardMenu.map((section) => (
    <div class="space-y-3">
      {section.groupLabel ? (
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-[hsl(var(--foreground)/0.65)]">
          {section.groupLabel}
        </p>
      ) : null}

      <div class="space-y-2">
        {section.menus.map((item) => (
          <a
            href={item.href}
            class="block rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--secondary))] px-4 py-3 text-sm font-medium text-[hsl(var(--foreground))] hover:bg-[hsl(var(--background))] transition-colors"
          >
            {item.label}
          </a>
        ))}
      </div>
    </div>
  ))}

  <SidebarTheming />
</nav>
